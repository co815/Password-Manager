server:
  ssl:
    enabled: false

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
  data:
    mongodb:
      uri: mongodb://localhost:27017/test
      database: test

app:
  jwt:
    secret: test-secret-value-test-secret-value
  auth:
    captcha:
      provider: none
    cookie:
      same-site: Strict
    placeholder-salt-secret: test-placeholder-secret
  cors:
    origins: [ ]